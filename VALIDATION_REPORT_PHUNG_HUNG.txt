PHÙNG HƯNG — VALIDATION REPORT (v3 fix)
========================================
Character: Phùng Hưng
Slug: phung-hung

Fixes in this revision:
- Restored runtime-compatible `sources` arrays alongside required `sourceIds` for profile/timeline/facts.
- Removed character-specific hard-coded QA/What-if suggestions from HTML/app.js; suggestions are rendered from phung-hung.json as in the Trưng Trắc source template.
- Restored functioning “Cách khám phá” guide rendering.
- Restored functioning “Nguồn sử liệu” list rendering.
- Completed VI/EN labels for every source card.
- Removed Wikisource runtime URLs and replaced them with permitted-source links (National Library of Vietnam / Người Kể Sử / Nghiên Cứu Lịch Sử) where a verified link is available.
- Source entries without a verified URL remain listed without a fabricated link.
- Validator now fails if runtime `sources` and required `sourceIds` diverge or source VI/EN labels are incomplete.

Executed checks:
- node --check public/app.js : PASS
- node --check server.js     : PASS
- npm run validate           : PASS (87 checks)

Browser execution note:
- Chromium headless is not functional in this execution environment (it hangs even on a data: URL), so no browser-pass claim is made here.
- npm install was attempted but timed out in this environment; partial node_modules/package-lock were removed before packaging.
